/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.fineartamerica.com",
      "kagi.com",
      "localhost",
      "i.imgur.com",
      "images.unsplash.com",
      "plus.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
